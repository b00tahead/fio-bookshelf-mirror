/**
 * @file
 * bookshelf behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.bookshelf = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
